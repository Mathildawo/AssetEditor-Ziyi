# AssetEditor-Ziyi
Fixed certain functions from official version 0.63

Keyframe editor restored (improved interpolation)

Animation re-Target tool (batch processing restored as in version 0.45)

Saving Animpack restored

Unresolved issue:

Deleting a file from the package can corrupt the package, which may cause ZSTD decompression errors
